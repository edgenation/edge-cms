doctype html
html(lang="en-GB")
    head
        meta(charset="UTF-8")

        if page
            title #{page.attributes.title} - Edge CMS
        else
            title Edge CMS
    body
        block content
