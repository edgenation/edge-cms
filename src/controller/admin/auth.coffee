auth =
    view: (req, res, next) ->
        # TODO: Show the login form
        
    login: (req, res, next) ->
        # Log the user in
        
    logout: (req, res, next) ->
        # Log the user out


module.exports = auth