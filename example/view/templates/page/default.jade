extends /layout/main

block content

    h1 Example
    h2 #{page.title}

    each region in page.regions
        div
            h2= region.name
            p
                + content-for-region-name(region.name, page)
