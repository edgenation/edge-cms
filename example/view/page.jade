extends /layout/main

block content

    h1 Example
    h2 #{page.attributes.title}


    ul
        each region in page.attributes.regions
            li #{region.id} - #{region.type}
            ul
                each content in region.attributes.content
                    li #{content.id} - #{content.type}
